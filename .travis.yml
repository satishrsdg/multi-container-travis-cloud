sudo: required
services:
  - docker

before_install:
  - docker build -t satishrsdg/react-test -f ./client/Dockerfile.dev ./client

script:
  # ensures npm test exits after test -- --coverage
  - docker run satishrsdg/react-test npm test -- --coverage

after_success:
  - docker build -t satishrsdg/multi-client ./client
  - docker build -t satishrsdg/multi-nginx ./nginx
  - docker build -t satishrsdg/multi-server ./server
  - docker build -t satishrsdg/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASS" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push satishrsdg/multi-client
  - docker push satishrsdg/multi-nginx
  - docker push satishrsdg/multi-server
  - docker push satishrsdg/multi-worker

# deploy:
#   provider: elasticbeanstalk
#   region: us-west-1
#   app: multi-docker
#   env: MultiDocker-env
#   bucket_name: elasticbeanstalk-us-west-1-306476627547
#   bucket_path: docker-multi
#   on:
#     branch: master
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key:
#     secure: $AWS_SECRET_KE